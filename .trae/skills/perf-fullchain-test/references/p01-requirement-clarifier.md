# P01 需求澄清 — perf-requirement-clarifier

## 解决的问题

性能需求阶段常见问题：SLA 没书面确认、并发数拍脑袋、环境差异没评估、链路范围未对齐，导致压测完返工重跑。

## 触发词

- 性能需求澄清 / 压测需求确认 / 确认压测目标
- perf requirement clarifier / clarify perf requirements

## 输入

- 接口文档 / PRD / 口头需求描述均可，无需提前整理格式

## 输出（自动判断数量）

| 文件 | 说明 |
|------|------|
| ① 需求澄清文档（主文档） | 10 大固定章节 |
| ② 待确认项清单（P0/P1 分级） | 防甩锅专用 |
| ③ 业务方确认函 | 可直接转发 |
| ④ 需求摘要 | 一页纸版本，给领导 |

---

## ① 需求澄清文档（10 大模块）

编号 ①~⑩ 不得遗漏、不得调换顺序：

| 编号 | 章节 | 内容 |
|------|------|------|
| ① | 项目基本信息 | 含 plan_id 命名规范 |
| ② | 压测目标分类 | 4 类目标说明（基线/容量/稳定性/极限） |
| ③ | 压测范围 | 接口清单 + 链路拓扑 + 参数场景 + 异常场景 |
| ④ | 负载模型 | 按目标类型自动选择 |
| ⑤ | 全量 SLA 指标 | 见下方 SLA 表 |
| ⑥ | 环境说明 | 含差异折算 |
| ⑦ | 验收标准 | Pass/Fail/Pass with risk |
| ⑧ | 前置风险 & 待确认项 | 见下方待确认清单 |
| ⑨ | 业务约束 | 时间段、资源限制 |
| ⑩ | 历史基线文档 | 对比样本 |

### SLA 推荐值表

业务方说不出 SLA 时，直接提供下表：

| 场景类型 | 测试环境 P95 | 生产环境 P95 | 错误率 |
|----------|-------------|-------------|--------|
| 简单查询（单条） | ≤ 300ms | ≤ 200ms | ≤ 0.1% |
| 列表查询（普通分页） | ≤ 800ms | ≤ 500ms | ≤ 0.1% |
| 列表查询（大数据分页） | ≤ 1500ms | ≤ 1000ms | ≤ 0.5% |
| 游标分页 / 深度分页 | ≤ 2000ms | ≤ 1500ms | ≤ 1% |
| 写入操作 | ≤ 800ms | ≤ 500ms | ≤ 0.1% |
| RPC 跨服务调用 | ≤ 1000ms | ≤ 800ms | ≤ 0.5% |
| MQ 异步回调 | ≤ 5000ms | ≤ 3000ms | ≤ 1% |
| 文件上传/下载 | ≤ 3000ms | ≤ 2000ms | ≤ 1% |

> 测试环境阈值比生产放宽 30%~50%，避免直接把测试指标当生产 SLA 用。

### 环境差异折算公式

```
测试环境 2C4G → 生产环境 4C8G → CPU ×1.8, 内存 ×1.2
测试环境 TPS=500 → 预估生产 TPS ≈ 500 × 1.8 × 1.2 ≈ 1080
```

### 负载默认值规则

业务方只给并发不给时长时，自动填充：

| 压测类型 | 默认持续时长 | Ramp-up 默认规则 |
|----------|-------------|-----------------|
| 基线测试 | 5 分钟 | ramp-up = 并发数 / 5（秒），最低 60 秒 |
| 容量规划 | 每阶梯 5~10 分钟 | 每阶梯 ramp-up = 阶梯并发增量 / 5 |
| 稳定性测试 | 12 小时 | ramp-up = 总并发 / 5，最低 5 分钟 |

---

## ② 待确认项清单（P0/P1 分级）

自动扫描文档中所有"假设/默认值/未验证"字段，按风险分级：

| 序号 | 待确认问题 | 风险等级 | 建议确认人 |
|------|-----------|---------|-----------|
| 1 | 并发数的数据来源？峰值还是均值？ | P0 | 产品经理 |
| 2 | P95/P99 及错误率是否与业务方书面确认？ | P0 | 产品经理 |
| 3 | 吞吐量目标 QPS/TPS 是否已确定？ | P0 | 产品经理 |
| 4 | 测试环境数据库/中间件是否与生产同规格？ | P0 | 运维 |
| 5 | 压测是否涉及写操作？数据清理方案？ | P0 | 开发 |
| 6 | 写接口幂等性、事务回滚是否已实现？ | P0 | 开发 |
| 7 | 依赖的外部系统是否参与压测？Mock 方案？ | P1 | 架构师 |
| 8 | 压测时间段是否有业务限制？ | P1 | 运维 |
| 9 | 监控大盘是否已覆盖资源类指标？ | P1 | 运维 |
| 10 | 分页/批量/文件等特殊参数场景是否已对齐？ | P1 | 开发/产品 |

> P0 项为阻塞项：任何一项未确认，压测不得启动。

---

## ③ 业务方确认函模板

```
我们计划对 [接口名称] 进行压测，预计 [日期] 执行，核心接口包括：[接口列表]。

需要你确认：
1. 并发数 [X] 及吞吐量目标 [X QPS] 是否合理？是否等于业务峰值流量？
2. 响应时间要求 P95 ≤ [X]ms / P99 ≤ [X]ms 是否可接受？
3. 压测范围是否完整？参数场景（分页/批量/文件）是否已对齐？
4. 写接口验收标准是否可接受？（库存最终一致性、幂等性、事务回滚正确性）
5. 是否接受测试环境结果作为参考？（测试环境性能约为生产的 60-70%，结果需折算）

请回复：
✅ 确认以上内容，可按计划执行
❓ 有以下修改意见：[请填写]
```

---

## ④ 需求摘要模板

```
# 压测需求摘要 - [项目名]

## 压测目标
[一句话描述]

## 核心指标
- 并发数: [X]
- TPS 目标: [X]
- P95: ≤ [X]ms
- P99: ≤ [X]ms
- 错误率: ≤ [X]%

## 压测范围
[接口列表 - 简要]

## 环境
[测试环境规格，生产环境规格]

## 风险项
[关键风险，最多 3 条]
```

---

## 前置条件检查

需求澄清前确认 7 项基本条件：

1. 压测目标是否明确（基线/容量/稳定性/极限）
2. 接口清单是否完整
3. SLA 指标是否书面确认
4. 测试环境是否可用
5. 压测时间段是否确定
6. 数据准备方案是否对齐
7. 依赖的外部服务处理方案（Mock/参与/跳过）

> 任何一项不满足 → 建议暂停，补齐后再推进。

---

## 跨平台 Prompt 模板

```
你是一个性能测试需求分析师。你的任务是根据用户提供的需求描述，输出标准化的性能测试需求文档。

请严格按以下步骤执行：

第一步：前置条件检查
检查以下 7 项是否就绪：压测目标明确、接口清单完整、SLA 确认、环境可用、时间段确定、数据方案对齐、外部服务方案。任一缺失，列出缺失项并建议暂停。

第二步：追问关键信息
- 并发数的数据来源（峰值还是均值）？
- P95/P99 是否与业务方书面确认？
- 测试环境的数据库/中间件规格是否与生产一致？
- 写接口的幂等性和事务回滚是否实现？

第三步：输出正式文档
1. 需求澄清文档（含以下章节）
2. 待确认项清单（P0/P1 分级，P0 阻塞不得启动）
3. 业务方确认函（可直接转发）
4. 需求摘要（一页纸）

需求澄清文档的 10 个模块（严格按顺序）：
① 项目基本信息 → ② 压测目标分类 → ③ 压测范围（接口清单+链路拓扑+参数场景+异常场景）
→ ④ 负载模型（按目标类型自动选择） → ⑤ 全量 SLA 指标（提供 SLA 推荐值表）
→ ⑥ 环境说明（含差异折算公式） → ⑦ 验收标准 → ⑧ 前置风险 & 待确认项
→ ⑨ 业务约束 → ⑩ 历史基线文档

SLA 推荐值表规则：
| 场景类型 | 测试环境 P95 | 生产环境 P95 | 错误率 |
| 简单查询(单条) | ≤300ms | ≤200ms | ≤0.1% |
| 列表查询(普通分页) | ≤800ms | ≤500ms | ≤0.1% |
| 写入操作 | ≤800ms | ≤500ms | ≤0.1% |
| RPC跨服务调用 | ≤1000ms | ≤800ms | ≤0.5% |

环境差异折算公式：测试环境 XC YG → 生产环境 X'C Y'G → CPU 系数 ×(X'/X)^0.8, 内存系数 ×(Y'/Y)^0.2

负载默认值：基线测试 5 分钟，ramp-up=并发数/5(最低60秒)；容量规划每阶梯5-10分钟；稳定性测试12小时，ramp-up=总并发/5(最低5分钟)。

P0 阻塞项定义（任何一项未确认不得启动）：
1. 并发数数据来源未确认
2. P95/P99/错误率未与业务方书面确认
3. 吞吐量目标未确定
4. 环境差异未评估
5. 写操作清理方案未确定
6. 写接口幂等性未实现
```